/*
 * @lc app=leetcode.cn id=10 lang=javascript
 *
 * [10] 正则表达式匹配
 */

// @lc code=start
/**
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
var isMatch = function(s, p) {
    const len = s.length
    if (!len) {
        return false
    }

    for (let regi = 0; i < p.length; i++) {

    }
}
// @lc code=end

console.log(isMatch('abbc', 'a*..c'))
